# ============================================
# Configuration - Projet Amazon Webscraping
# ISSEA MDSMS2 - 2025/2026
#
# Copier ce fichier en .env et remplir les valeurs
# ============================================

# === BASE DE DONNÉES ===
# Type : 'sqlite' (local) ou 'postgresql' (Cloud SQL)
DB_TYPE=sqlite

# SQLite (local/Cloud Run avec volume)
DB_PATH=amazon_scraping.db

# PostgreSQL (Cloud SQL - décommenter si utilisé)
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_NAME=amazon_db
# DB_USER=postgres
# DB_PASSWORD=votre_mot_de_passe

# === SCRAPING ===
# Mode headless (true pour Cloud Run, false pour debug local)
HEADLESS=true

# Type de navigateur : 'chrome' (Cloud Run) ou 'edge' (local Windows)
BROWSER=chrome

# === ALERTES EMAIL ===
# Serveur SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Identifiants email (Gmail recommandé avec App Password)
# ⚠️ Ne JAMAIS commiter le vrai .env avec ces valeurs !
SMTP_USER=votre.email@gmail.com
SMTP_PASSWORD=votre_app_password_gmail

# Destinataire des alertes
EMAIL_DESTINATAIRE=votre.email@gmail.com

# Seuils d'alerte
SEUIL_BAISSE_PRIX=10.0
SEUIL_RAPPORT_QP=8.0

# === CLOUD (optionnel) ===
# Google Cloud Project ID
# GCP_PROJECT_ID=votre-projet-id

# Cloud Storage Bucket (optionnel)
# GCS_BUCKET=votre-bucket-name

# === APPLICATION ===
# Port (automatiquement défini par Cloud Run)
PORT=8080
